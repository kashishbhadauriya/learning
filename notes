Can this be sorted by rotations?
for(int i=0;i<n;i++){
    if(nums[(startidx+i)%n] != i)
        return false;
}
return true;


standard way to check whether two intervals overlap:-
if (max(firstfirst, secondfirst) <= min(firstsecond, secondsecond))
ex-[2, 8]
[5,10]

The overlap starts at 5, not 2.

max(2,5) = 5
